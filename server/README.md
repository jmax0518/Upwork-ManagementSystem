# Legacy Express API (deprecated)

The active platform is the Next.js app in **`../web`**. It exposes the same `/api/jobs` contract on port **3847** and includes a simple inbox UI.

You can delete this folder once you no longer need the standalone Express server.

```bash
cd ../web
npm install
npm run dev
```
